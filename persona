#!/bin/bash

if [ $# -eq 0 ]; then
    echo "usage: persona <persona-name>"
    exit 1
fi

persona=$1
shift

if [ ! -f "personas/${persona}.md" ]; then
    echo "Persona ${persona} does not exist"
    exit 1
fi

CLAUDE_PERSONA=mentor claude --system-prompt-file "personas/${persona}.md" "$@"
